# # = called sharp, ! = called a bang, #! = called schbang for including the bash
# use which bash to find where your bash located

# make the file readable and enable permissions by using change mode: 

# chmod +x (name of the file) simple-echo.sh <-- makes it executable with permission

# to run the program with bash use ./ (filename) eg. ./simple-echo.sh

#! usr/bin/bash

# ECHO Command

echo Hello World!